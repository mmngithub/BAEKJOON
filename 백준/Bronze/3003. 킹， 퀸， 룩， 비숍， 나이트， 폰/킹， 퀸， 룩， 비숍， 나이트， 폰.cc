#define _CRT_SECURE_NO_WARNINGS
#include<iostream>
using namespace std;

int main() {
	int a = 1, b = 1, c = 2, d = 2, e = 2, f = 8;
	int q, w, i, r, t,u;
	scanf("%d %d %d %d %d %d", &q, &w, &i, &r, &t, &u);
	cout << a-q << " " << b-w << " " << c-i << " " << d-r << " " << e-t << " " << f-u;

}